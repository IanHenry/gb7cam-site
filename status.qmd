---
title: "System Status"
---

<img src="images/main_logo.png" class="centered-image">

## Current Status

<div id="api-status-widget" class="status-card">
  <div><strong>Status:</strong> <span id="w-status">Operational</span></div>
  <div><strong>Active Links:</strong> <span id="w-links">...</span></div>
  <div><strong>Stations Heard via RF (last 24 hours):</strong> <span id="w-rf">...</span></div>
  <div><strong>Stations Heard via AXIP (last 24 hours):</strong> <span id="w-axip">...</span></div>
  <div><strong>Last Packet:</strong> <span id="w-last">...</span></div>
</div>

---

## Activity Feed
<div id="activity-feed-container" class="panel">
  <p>Loading...</p>
</div>

---

## Live Links
<div id="links-container" class="panel">
  <p>Loading...</p>
</div>

---

## Live MHeard
<div id="mheard-container" class="panel">
  <p>Loading...</p>
</div>

---

## Network Routing Nodes

<input id="node-search" class="search-box" placeholder="Search nodes...">

<div id="nodes-container" class="panel nodes">
  <p>Loading routing node index...</p>
</div>

<script>
let allNodes = [];

/* ----------------------------
   CONSTANTS
---------------------------- */
const RF_PORT = 1;
const AXIP_PORT = 3;
const WINDOW_MS = 24 * 60 * 60 * 1000;

/* ----------------------------
   HELPERS
---------------------------- */
function parseTime(st) {
  if (!st?.lastHeard) return NaN;

  const [datePart, timePart] = st.lastHeard.split(" ");
  if (!datePart || !timePart) return NaN;

  const [y, m, d] = datePart.split("-").map(Number);
  const [hh, mm, ss = 0] = timePart.split(":").map(Number);

  return Date.UTC(y, m - 1, d, hh, mm, ss);
}

function formatUTC(ts) {
  return new Date(ts)
    .toISOString()
    .substring(0, 19)
    .replace("T", " ") + " UTC";
}

function portColor(port) {
  return port === RF_PORT ? "#2e7d32" : "#1565c0";
}

/* ----------------------------
   FETCH DATA
---------------------------- */
async function fetchDashboardData() {
  const base = "https://api.gb7cam.org.uk";

  const [linksRes, mheardRes, nodesRes] = await Promise.all([
    fetch(base + "/links"),
    fetch(base + "/mheard/all"),
    fetch(base + "/nodes")
  ]);

  if (!linksRes.ok || !mheardRes.ok || !nodesRes.ok)
    throw new Error("API failure");

  const links = (await linksRes.json()).links || [];
  const rawMheard = (await mheardRes.json()).mheard || [];
  allNodes = (await nodesRes.json()).nodes || [];

  const cutoff = Date.now() - WINDOW_MS;

  const mheardAll = rawMheard
    .map(st => ({ ...st, ts: parseTime(st) }))
    .filter(st => !isNaN(st.ts) && st.ts >= cutoff)
    .sort((a, b) => b.ts - a.ts);

  /* ----------------------------
     ACTIVITY FEED
  ---------------------------- */
  document.getElementById("activity-feed-container").innerHTML =
    mheardAll.length === 0
      ? "<p style='color:#666;'>No recent activity</p>"
      : mheardAll.map(st => {
          const port = Number(st.port);
          return `
            <div style="margin-bottom:4px;">
              🕒 ${formatUTC(st.ts)}
              <span style="color:${portColor(port)};font-weight:bold;">
                Heard ${st.callSign}
              </span>
              on Port ${st.port}
            </div>`;
        }).join("");

  /* ----------------------------
     LINKS
  ---------------------------- */
  document.getElementById("links-container").innerHTML =
    links.length === 0
      ? "<p>No active links</p>"
      : `
      <table style="width:100%;border-collapse:collapse;font-family:monospace;">
        <tr style="background:#f1f1f1;">
          <th>Node</th><th>Port</th><th>Type</th><th>Status</th>
        </tr>
        ${links.map(l => {
          const up = ["ACTIVE","ESTABLISHED","CONNECTED"]
            .includes((l.state||"").toUpperCase());

          return `
            <tr>
              <td>${l.farCall}</td>
              <td>${l.port}</td>
              <td>${l.linkType || "AXUDP"}</td>
              <td>
                <span style="
                  padding:3px 8px;
                  border-radius:4px;
                  font-size:12px;
                  color:${up ? "#155724" : "#721c24"};
                  background:${up ? "#d4edda" : "#f8d7da"};
                  font-weight:bold;">
                  ${l.state || "ACTIVE"}
                </span>
              </td>
            </tr>`;
        }).join("")}
      </table>`;

  /* ----------------------------
     MHEARD (UNIFIED)
  ---------------------------- */
  document.getElementById("mheard-container").innerHTML =
    mheardAll.length === 0
      ? "<p>No stations</p>"
      : `
      <table style="width:100%;border-collapse:collapse;font-family:monospace;">
        <tr style="background:#f1f1f1;">
          <th>Call</th><th>Port</th><th>Packets</th><th>Last Heard</th>
        </tr>
        ${mheardAll.map(st => {
          const port = Number(st.port);
          return `
            <tr>
              <td style="color:${portColor(port)};font-weight:bold;">
                ${st.callSign}
              </td>
              <td>${st.port}</td>
              <td>${Number(st.packets||0).toLocaleString()}</td>
              <td>${formatUTC(st.ts)}</td>
            </tr>`;
        }).join("")}
      </table>`;

  /* ----------------------------
     METRICS (SINGLE PASS)
  ---------------------------- */
  let rf = 0, axip = 0, latest = 0, latestCall = "";

  for (const st of mheardAll) {
    const port = Number(st.port);

    if (st.ts > latest) {
      latest = st.ts;
      latestCall = st.callSign;
    }

    if (port === RF_PORT) rf++;
    if (port === AXIP_PORT) axip++;
  }

  document.getElementById("w-links").innerText = links.length;
  document.getElementById("w-rf").innerText = rf;
  document.getElementById("w-axip").innerText = axip;

  document.getElementById("w-last").innerText =
    latest
      ? `${latestCall} - ${Math.floor((Date.now()-latest)/60000)} mins ago`
      : "No data";

  document.getElementById("w-status").innerText = "Operational";

  renderNodes(allNodes);
}

/* ----------------------------
   NODES
---------------------------- */
function renderNodes(list) {
  const el = document.getElementById("nodes-container");

  if (!list.length) {
    el.innerHTML = "<p style='color:#666;'>No matching nodes</p>";
    return;
  }

  el.innerHTML = `
    <div style="
      display:grid;
      grid-template-columns:repeat(auto-fill, minmax(220px, 1fr));
      gap:10px;
    ">
      ${list.map(n => `
        <div style="
          background:white;
          border:1px solid #e2e8f0;
          padding:10px 12px;
          border-radius:6px;
          box-shadow:0 1px 2px rgba(0,0,0,0.04);
        ">
          <div style="font-weight:bold; color:#2c3e50;">
            ${n.Call}
            ${n.Alias ? `<span style="color:#e67e22;"> :${n.Alias}</span>` : ""}
          </div>

          <div style="font-size:0.85em;color:#7f8c8d;">
            🗺️ Routes: <strong>${n.Routes?.length || 0}</strong>
          </div>
        </div>
      `).join("")}
    </div>
  `;
}

/* ----------------------------
   EVENTS
---------------------------- */
document.addEventListener("DOMContentLoaded", () => {
  fetchDashboardData();
  setInterval(fetchDashboardData, 60000);

  document.getElementById("node-search").addEventListener("input", e => {
    const t = e.target.value.toUpperCase();
    renderNodes(allNodes.filter(n =>
      n.Call.toUpperCase().includes(t) ||
      (n.Alias || "").toUpperCase().includes(t)
    ));
  });
});
</script>